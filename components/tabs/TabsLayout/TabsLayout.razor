@namespace AntDesign
@inherits LayoutComponentBase

<Tabs @bind-ActiveKey="_activeKey">
    @foreach (var pane in _tabPages)
    {
        <TabPane Key="@pane.Key">
            <Tab>@pane.Key</Tab>
            <ChildContent>
                @pane.Value._pageBody
            </ChildContent>
        </TabPane>
    }
</Tabs>